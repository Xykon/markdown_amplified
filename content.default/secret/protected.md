# Protected Page

You made it in! This page is protected by the **folder password**.

Any file inside the `/secret` directory that doesn't have its own rule inherits the directory password: `secret`.

This is useful when you have a collection of related content you want to gate with a single password — you only need one rule in `content-security.json` for the whole folder.

---

[← Back to secret index](/secret/)
